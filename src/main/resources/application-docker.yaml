spring:
  application:
    name: jenkins

  datasource:
    url: jdbc:mysql://mysql-db:3306/testdb
    username: root
    password: docker
    driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      show-sql: true